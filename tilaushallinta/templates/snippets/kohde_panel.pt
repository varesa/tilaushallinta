<div tal:define="compact compact|False; readonly readonly|False">
    Nimi:<br>
    <input name="kohde_nimi"      value="${tilaus.kohde.nimi|None}" readonly="${None if not readonly else 'readonly'}">
    <br tal:condition="not: compact"></br>
    Yritys:<br>
    <input name="kohde_yritys"    value="${tilaus.kohde.yritys|None}" readonly="${None if not readonly else 'readonly'}">
    <br tal:condition="not: compact"></br>
    Y-tunnus:<br>
    <input name="kohde_ytunnus"   value="${tilaus.kohde.ytunnus|None}" readonly="${None if not readonly else 'readonly'}">
    <br tal:condition="not: compact"></br>
    Osoite:<br>
    <input name="kohde_osoite"    value="${tilaus.kohde.osoite|None}" readonly="${None if not readonly else 'readonly'}">
    <br tal:condition="not: compact"></br>
    Postitoimipaikka:<br>
    <input name="kohde_postitoimipaikka"  value="${tilaus.kohde.postitoimipaikka|None}" readonly="${None if not readonly else 'readonly'}">
    <br tal:condition="not: compact"></br>
    Postinumero:<br>
    <input name="kohde_postinumero"       value="${tilaus.kohde.postinumero|None}" readonly="${None if not readonly else 'readonly'}">
    <br tal:condition="not: compact"></br>
    Puhelin:<br>
    <input name="kohde_puh"       value="${tilaus.kohde.puhelin|None}" readonly="${None if not readonly else 'readonly'}">
    <br tal:condition="not: compact"></br>
    Sähköposti:<br>
    <input name="kohde_email"     value="${tilaus.kohde.email|None}" readonly="${None if not readonly else 'readonly'}">
</div>